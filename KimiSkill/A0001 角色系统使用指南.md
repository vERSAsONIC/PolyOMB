# 角色系统使用指南

> 本文档说明如何使用 Kimi CLI 的多角色系统

---

## 📦 已安装的角色列表（共 8 个）

### 🎭 管理类

#### 🎭 角色调度员 (role-orchestrator)
| 属性 | 内容 |
|------|------|
| **激活词** | `role orchestrator`, "帮我选个角色", "列出所有角色" |
| **权限** | 👁️ read |
| **特色** | 中央管理，帮助选择合适角色 |
| **适合场景** | 不确定用哪个角色时；查看所有可用角色 |

---

### 🔍 分析类（只读权限）

#### 🔍 项目研究员 (role-researcher)
| 属性 | 内容 |
|------|------|
| **激活词** | `role researcher`, "切换到研究员", "分析项目" |
| **权限** | 👁️ read |
| **特色** | 系统性代码分析，输出结构化调研报告 |
| **适合场景** | 刚接触新项目；调研功能实现；梳理依赖关系 |

#### 📝 代码审查员 (role-code-reviewer)
| 属性 | 内容 |
|------|------|
| **激活词** | `role reviewer`, "切换到审查员", "code review" |
| **权限** | 👁️ read |
| **特色** | 多维度代码审查（质量/安全/性能），分级问题输出 |
| **适合场景** | 提交前代码审查；学习代码规范；发现潜在 bug |

---

### ✍️ 创作类（读写权限）

#### 🧩 Skill 作者 (role-skill-writer)
| 属性 | 内容 |
|------|------|
| **激活词** | `role skill-writer`, "切换到 Skill 作者", "创建 skill" |
| **权限** | 👁️ read + ✏️ edit + 🖥️ command |
| **特色** | 熟悉 SKILL.md 规范，支持渐进式披露设计 |
| **适合场景** | 创建新的 Kimi Skill；编辑现有 Skill；验证规范 |

#### ✍️ Mode 作者 (role-mode-writer)
| 属性 | 内容 |
|------|------|
| **激活词** | `role mode-writer`, "切换到 Mode 作者", "创建 mode" |
| **权限** | 👁️ read + ✏️ edit + 🖥️ command |
| **特色** | 设计角色定义和工具权限，确保 Mode 间一致性 |
| **适合场景** | 创建新的 Custom Mode；编辑现有 Mode；设计权限组 |

#### 🏗️ 架构师 (role-architect)
| 属性 | 内容 |
|------|------|
| **激活词** | `role architect`, "切换到架构师", "设计系统" |
| **权限** | 👁️ read + 🌐 browser |
| **特色** | 系统设计、技术选型，可查阅外部资料 |
| **适合场景** | 系统架构设计；技术决策；性能规划；架构演进 |

### 🎯 咨询类（特殊交互模式）

#### 🎯 顾问 (role-consultant)
| 属性 | 内容 |
|------|------|
| **激活词** | `role consultant`, "切换到顾问", "帮我设计软件" |
| **权限** | 👁️ read + 🌐 browser + ✏️ 限制（仅文档） |
| **特色** | 面向非技术用户，可视化设计（Mermaid 图表），**分段输入模式** |
| **适合场景** | 需求分析、架构设计、技术选型；适合逐条输入需求的用户 |
| **交互方式** | 用户逐条提供信息 → 顾问回应"了解，还有呢？" → 触发词统一输出 |
| **输出触发词** | "你有什么看法" / "你怎么看" / "总结一下" / "给我方案" |

**使用示例**：
```
用户：我想做一个记账软件
顾问：了解，还有呢？
用户：可以记录收入和支出
顾问：了解，还有呢？
用户：你有什么看法？
顾问：[输出完整架构设计]
```

### 📚 文档类（全文读写权限）

#### 📚 文档作者 (role-doc-writer)
| 属性 | 内容 |
|------|------|
| **激活词** | `role doc-writer`, "切换到文档作者", "帮我写文档" |
| **权限** | 👁️ read + ✏️ edit + 🖥️ command + 🌐 browser |
| **特色** | 专业文档工程师，多种文档模板，风格统一 |
| **适合场景** | 编写 README、API 文档、使用指南、技术规范 |

---

## 🚀 快速开始

### 方式 1：直接说激活词（最简单）

在对话中直接输入：
```
帮我选个角色
```
或：
```
role researcher
```

### 方式 2：启动时加载

```bash
# 启动 Kimi 并加载特定角色
kimi --skill role-researcher

# 加载多个角色
kimi --skill role-researcher --skill role-orchestrator
```

### 方式 3：查看所有角色

```bash
ls ~/.config/agents/skills/
```

---

## 🔐 权限说明

### 权限图标

| 图标 | 名称 | 可用工具 |
|------|------|----------|
| 👁️ | **read** | ReadFile, Glob, Grep, ReadMediaFile |
| ✏️ | **edit** | WriteFile, StrReplaceFile |
| 🖥️ | **command** | Shell |
| 🌐 | **browser** | FetchURL, SearchWeb |

### 角色权限矩阵

| 角色 | 👁️ | ✏️ | 🖥️ | 🌐 | 安全级别 |
|------|-----|-----|-----|-----|----------|
| 🎭 角色调度员 | ✅ | ❌ | ❌ | ❌ | ⭐⭐⭐⭐⭐ |
| 🔍 项目研究员 | ✅ | ❌ | ❌ | ❌ | ⭐⭐⭐⭐⭐ |
| 📝 代码审查员 | ✅ | ❌ | ❌ | ❌ | ⭐⭐⭐⭐⭐ |
| 🧩 Skill 作者 | ✅ | ✅ | ✅ | ❌ | ⭐⭐⭐ |
| ✍️ Mode 作者 | ✅ | ✅ | ✅ | ❌ | ⭐⭐⭐ |
| 🏗️ 架构师 | ✅ | ❌ | ❌ | ✅ | ⭐⭐⭐⭐ |
| 📚 文档作者 | ✅ | ✅ | ✅ | ✅ | ⭐⭐⭐ |

---

## 🎯 场景选择指南

### 我是新手，刚接触项目
```
role researcher → 分析项目结构和架构
```

### 我要提交代码，想先检查一遍
```
role reviewer → 进行代码审查
```

### 我要设计新功能，不确定技术方案
```
role architect → 系统设计和技术选型
```

### 我要写文档
```
role doc-writer → 编写 README、API 文档、使用指南
```

### 我要创建自动化工具
```
role skill-writer → 创建 Kimi Skill
```

### 我要定义新的工作角色
```
role mode-writer → 创建 Custom Mode
```

### 我不确定该用哪个角色
```
role orchestrator → 角色调度员会帮你选择
```

---

## 🔄 典型工作流示例

### 工作流 1：接手新项目

```bash
# 1. 启动研究员角色了解项目
kimi --skill role-researcher
```

对话：
```
> 切换到研究员
> 帮我分析一下这个项目的架构
```

### 工作流 2：开发新功能

```bash
# 1. 先用架构师设计
kimi --skill role-architect
```

对话：
```
> 切换到架构师
> 我要添加用户系统，帮我设计一下
```

然后：
```bash
# 2. 编码实现（默认角色或自定义开发角色）
kimi
```

### 工作流 3：代码审查流程

```bash
# 使用审查员角色检查代码
kimi --skill role-code-reviewer
```

对话：
```
> 切换到审查员
> 帮我审查 src/auth/ 目录下的代码
```

### 工作流 4：创建自己的工具

```bash
# 1. 先用调度员了解 Skill 系统
kimi --skill role-orchestrator

# 2. 然后用 Skill 作者创建
kimi --skill role-skill-writer
```

对话：
```
> 切换到 Skill 作者
> 帮我创建一个处理 Markdown 的 skill
```

---

## 🛠️ 管理角色

### 查看已安装角色

```bash
ls -la ~/.config/agents/skills/
```

### 编辑角色定义

```bash
# 编辑角色调度员
vim ~/.config/agents/skills/role-orchestrator/SKILL.md

# 编辑项目研究员
vim ~/.config/agents/skills/role-researcher/SKILL.md

# 编辑代码审查员
vim ~/.config/agents/skills/role-code-reviewer/SKILL.md

# 编辑 Skill 作者
vim ~/.config/agents/skills/role-skill-writer/SKILL.md

# 编辑 Mode 作者
vim ~/.config/agents/skills/role-mode-writer/SKILL.md

# 编辑架构师
vim ~/.config/agents/skills/role-architect/SKILL.md
```

### 添加新角色

```bash
# 1. 创建目录
mkdir ~/.config/agents/skills/role-my-custom

# 2. 复制模板
cat > ~/.config/agents/skills/role-my-custom/SKILL.md << 'EOF'
---
name: role-my-custom
description: |
  🎭 切换到「我的自定义角色」。
  当用户说"切换到 XXX"、"role my-custom"时激活。
---

# 角色：我的自定义角色 🎭

## 角色定义
...

## 🔒 权限边界
| 类型 | 状态 |
|------|------|
| 👁️ read | ✅/❌ |
| ✏️ edit | ✅/❌ |
| 🖥️ command | ✅/❌ |
| 🌐 browser | ✅/❌ |
EOF
```

### 删除角色

```bash
rm -rf ~/.config/agents/skills/role-name
```

---

## ❓ 常见问题

**Q: 角色激活后怎么退出？**
A: 直接说"退出角色"或开启新对话。Kimi CLI 每次对话都是独立的。

**Q: 可以同时激活多个角色吗？**
A: 可以，启动时加载多个：
```bash
kimi --skill role-researcher --skill role-orchestrator
```

**Q: 角色和普通对话有什么区别？**
A: 角色会加载对应的 SKILL.md 作为系统提示词，影响 AI 的行为方式、工具选择和输出格式。

**Q: 为什么有些角色是只读的？**
A: 为了安全。分析类任务（调研、审查）不需要修改文件，只读权限可以防止误操作。

**Q: 我能修改角色的权限吗？**
A: 可以！角色权限只是文本约定，你可以编辑 SKILL.md 中的「权限边界」部分来调整。

**Q: 如何创建自己的角色？**
A: 最简单的方法：
1. 激活 `role-skill-writer`
2. 说"帮我创建一个名为 XXX 的角色"
3. 按照提示提供需求

---

## 📁 文件位置速查

| 角色 | 路径 |
|------|------|
| 🎭 角色调度员 | `~/.config/agents/skills/role-orchestrator/SKILL.md` |
| 🔍 项目研究员 | `~/.config/agents/skills/role-researcher/SKILL.md` |
| 📝 代码审查员 | `~/.config/agents/skills/role-code-reviewer/SKILL.md` |
| 🧩 Skill 作者 | `~/.config/agents/skills/role-skill-writer/SKILL.md` |
| ✍️ Mode 作者 | `~/.config/agents/skills/role-mode-writer/SKILL.md` |
| 🏗️ 架构师 | `~/.config/agents/skills/role-architect/SKILL.md` |
| 📚 文档作者 | `~/.config/agents/skills/role-doc-writer/SKILL.md` |
| 📖 本使用指南 | `A0001 角色系统使用指南.md` |

---

## 🎉 开始使用

现在就试试角色调度员：

```
帮我选个角色
```

或启动特定角色：

```bash
kimi --skill role-orchestrator
```

然后告诉我你想做什么任务，我会帮你选择最合适的角色！
