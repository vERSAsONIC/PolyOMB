# 角色系统迁移指南

> 如何在其他电脑上使用这些角色

---

## 📦 方案 1：使用 .skill 文件安装（最简单）

### 从本机导出

已为你打包好所有角色：
```
roles-complete.skill    ← 在本项目目录中
```

### 在新电脑上安装

```bash
# 1. 复制 roles-complete.skill 到新电脑
# 可以通过 U 盘、邮件、网盘等方式传输

# 2. 解压到 skills 目录
mkdir -p ~/.config/agents/skills
cd ~/.config/agents/skills
unzip ~/Downloads/roles-complete.skill   # 根据实际路径调整

# 3. 验证安装
ls ~/.config/agents/skills/
# 应该看到：role-architect, role-code-reviewer, role-doc-writer 等

# 4. 开始使用
kimi --skill role-orchestrator
```

---

## 🔄 方案 2：使用 Git 同步（推荐，持续同步）

### 创建 Git 仓库

```bash
# 1. 初始化仓库
cd ~/.config/agents/skills
git init
git add .
git commit -m "Initial commit: 7 roles"

# 2. 推送到 GitHub/GitLab
git remote add origin https://github.com/YOUR_USERNAME/kimi-roles.git
git push -u origin main
```

### 在新电脑上克隆

```bash
# 直接克隆到 skills 目录
rm -rf ~/.config/agents/skills
git clone https://github.com/YOUR_USERNAME/kimi-roles.git ~/.config/agents/skills

# 开始使用
kimi --skill role-researcher
```

### 后续更新

```bash
# 添加新角色后提交
cd ~/.config/agents/skills
git add .
git commit -m "Add new role: xxx"
git push

# 在其他电脑上同步
cd ~/.config/agents/skills
git pull
```

---

## ☁️ 方案 3：使用云同步（Dropbox/iCloud/OneDrive）

### 设置同步

```bash
# 1. 移动 skills 目录到云盘
mv ~/.config/agents/skills ~/Dropbox/kimi-skills/

# 2. 创建软链接
ln -s ~/Dropbox/kimi-skills ~/.config/agents/skills

# 3. 在其他电脑上同样设置
# 安装 Dropbox/iCloud → 同步完成后 → 创建软链接
```

### 优点
- 自动同步，无需手动操作
- 修改后立即同步到所有设备

---

## 💾 方案 4：使用 Dotfiles 管理器（极客推荐）

### 使用 chezmoi

```bash
# 1. 安装 chezmoi
brew install chezmoi

# 2. 初始化
chezmoi init

# 3. 添加 skills
chezmoi add ~/.config/agents/skills

# 4. 推送到 GitHub
chezmoi cd
git remote add origin https://github.com/YOUR_USERNAME/dotfiles.git
git push

# 5. 在新电脑上安装
sh -c "$(curl -fsLS get.chezmoi.io)" -- init --apply YOUR_USERNAME
```

### 使用 GNU Stow

```bash
# 创建 dotfiles 仓库结构
mkdir ~/dotfiles
cd ~/dotfiles
mkdir -p .config/agents
cp -r ~/.config/agents/skills .config/agents/

# 使用 stow 链接
cd ~
stow dotfiles
```

---

## 📋 各方案对比

| 方案 | 难度 | 同步方式 | 适用场景 |
|------|------|----------|----------|
| .skill 文件 | ⭐ 简单 | 手动拷贝 | 偶尔换电脑 |
| Git 仓库 | ⭐⭐ 中等 | 手动 pull/push | 经常更新，多设备 |
| 云盘同步 | ⭐ 简单 | 自动 | 已有云盘订阅 |
| Dotfiles | ⭐⭐⭐ 复杂 | 命令行 | 极客，完整配置管理 |

---

## 🚀 快速命令参考

### 导出（本机）
```bash
cd ~/.config/agents/skills && zip -r ~/roles-export.skill role-*
```

### 导入（新电脑）
```bash
mkdir -p ~/.config/agents/skills && cd ~/.config/agents/skills && unzip ~/roles-export.skill
```

### 验证安装
```bash
ls ~/.config/agents/skills/
kimi --skill role-orchestrator
```

---

## ❓ 常见问题

**Q: 其他电脑需要安装 Kimi CLI 吗？**
A: 需要！这些是 Kimi CLI 的 Skill，必须先安装 Kimi CLI。

**Q: 可以和同事共享这些角色吗？**
A: 可以！直接分享 `roles-complete.skill` 文件或 Git 仓库链接。

**Q: 不同操作系统（Mac/Linux/Windows）通用吗？**
A: 通用！Skill 是纯文本 Markdown 文件，跨平台兼容。

**Q: 修改后的角色怎么同步？**
A: 使用 Git 或云盘方案可以实现自动同步，.skill 文件需要重新打包。

---

## 📝 推荐方案总结

| 用户类型 | 推荐方案 |
|----------|----------|
| 偶尔换电脑 | .skill 文件（方案 1）|
| 经常更新角色 | Git 同步（方案 2）|
| 已有云盘订阅 | 云同步（方案 3）|
| 管理整套配置 | Dotfiles（方案 4）|

---

**现在，你的角色系统可以随身携带了！** 🎭
